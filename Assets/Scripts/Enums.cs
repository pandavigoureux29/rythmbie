public enum HitAccuracy
{
    PERFECT, GREAT, GOOD, MISS
}
public enum GameDifficulty
{
    EASY, NORMAL, HARD
}

public enum NoteType{ SIMPLE, LONG };

public enum ScoreAccuracy { PERFECT, GOOD, BAD, MISSED}

public enum NoteHitResult { NONE, HIT, MISSED}


public enum InputActionType { TAP_STARTED, TAP_RELEASED }
public enum InputRegion { LEFT, RIGHT}